<div class="shadow">
    <nav class="navbar-light navbar-expand-lg bg-light navbar">
        <a class="navbar-brand" href="#">
            <img src="{{ asset("images/mjpm.png") }}" height="56" width="56">
        </a>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul id="navbar" class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("user_majeurs") }}">
                        <i class="fas fa-users"></i> Majeurs
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("user_fichesdefrais") }}">
                        <i class="fas fa-receipt"></i> Fiches de frais
                    </a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown_liensexternes" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-link"></i> Liens externes
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown_liensexternes">
                        <a class="dropdown-item" href="#"><i class="fas fa-gavel"></i> Jugements</a>
                        <a class="dropdown-item" target="blank" href="https://dgcs-alt.social.gouv.fr/dgcs/ocmi"><i class="fas fa-gavel"></i> OCMI</a>
                    </div>
                </li>
            </ul>
            <ul id="navbar" class="navbar-nav">
                <li class="nav-item dropdown justify-content-end">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown_parametres" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-cog"></i> Paramétrage
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown_parametres">
                        <a class="dropdown-item" href="{{ path("user_agencebancaires") }}"><i class="fas fa-university"></i> Agences bancaires</a>
                        <a class="dropdown-item" href="{{ path("user_famillecomptes") }}"><i class="fas fa-list-alt"></i> Familles de compte</a>
                        <a class="dropdown-item" href="{{ path("user_familletypeoperations") }}"><i class="fas fa-balance-scale"></i> Familles de types d'opération</a>
                        <a class="dropdown-item" href="{{ path("user_lieu_vies") }}"><i class="fas fa-home"></i> Lieux de vie</a>
                        <a class="dropdown-item" href="{{ path("user_mesures") }}"><i class="fas fa-list"></i> Mesures</a>
                        <a class="dropdown-item" href="{{ path("user_pompefunebres") }}"><i class="fas fa-skull"></i> Pompes funèbres</a>
                        <a class="dropdown-item" href="{{ path("user_protections") }}"><i class="fas fa-shield-alt"></i> Protections</a>
                        <a class="dropdown-item" href="{{ path("user_tribunaux") }}"><i class="fas fa-gavel"></i> Tribunaux</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path("user_importoperations") }}"><i class="fas fa-file-csv"></i> Import d'opérations</a>
                        <div class="dropdown-divider"></div>
                        <div class="dropdown-header">TYPES</div>
                        <a class="dropdown-item" href="{{ path("user_typecomptes") }}"><i class="fas fa-university"></i> Comptes</a>
                        <a class="dropdown-item" href="{{ path("user_typesfrais") }}"><i class="fas fa-receipt"></i> Frais</a>
                        <a class="dropdown-item" href="{{ path("user_typesoperation") }}"><i class="fas fa-balance-scale"></i> Opérations</a>
                        <a class="dropdown-item" href="{{ path("user_typespriseencharge") }}"><i class="fas fa-life-ring"></i> Prises en charge</a>
                    </div>
                </li>
            </ul>
            {% if is_granted("ROLE_ADMIN") %}
            <ul id="navbar" class="navbar-nav">
                <li class="nav-item dropdown justify-content-end">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown_parametres" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-lock"></i> Administration
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown_parametres">
                        <a class="dropdown-item" href="{{ path("admin_users") }}"><i class="fas fa-user-lock"></i> Utilisateurs</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path("admin_mandataires") }}"><i class="fas fa-users-cog"></i> Mandataires</a>
                        <a class="dropdown-item" href="{{ path("admin_groupes") }}"><i class="fas fa-users-cog"></i> Groupes</a>
                    </div>
                </li>
            </ul>
            {% endif %}
            <ul id="navbar" class="navbar-nav">
                <li class="nav-item dropdown justify-content-end ml-3">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown_parametres" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-user"></i> {{ app.user.email }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown_parametres">
                        <a class="dropdown-item" href="{{ path("app_logout") }}"><i class="fas fa-sign-out-alt"></i> Déconnexion</a>
                    </div>
                </li>
            </ul>
        </div>
    </nav>
</div>