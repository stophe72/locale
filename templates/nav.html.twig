<div class="shadow">
    <nav class="navbar-light bg-light navbar-expand-lg navbar">
        <a class="navbar-brand" href="#">
            NJPM
        </a>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul id="navbar" class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("user_majeurs") }}">
                        Majeurs
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">
                        Notes de frais
                    </a>
                </li>
                {# <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown_rotations" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Rotations
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown_rotations">
                        <a class="dropdown-item" href="{{ path('rotations_parcelles') }}"><i class="text-success fas fa-chart-pie"></i> Par parcelles</a>
                        <a class="dropdown-item" href="{{ path('rotations_plantes') }}"><i class="text-success fa fa-seedling"></i> Par plantes</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('rotationsOuSemer') }}"><i class="text-success fas fa-map-marker-alt"></i> Où semer</a>
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown_commandes" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Commandes
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown_commandes">
                        <a class="dropdown-item" href="{{ path('commandes_graines_recapitulatif') }}"><i class="text-success fas fa-shopping-cart"></i> Liste</a>
                        <a class="dropdown-item" href="{{ path('commandes_graines_par_varietes') }}"><i class="text-success fas fa-shopping-cart"></i> Par variétés</a>
                    </div>
                </li> #}
            </ul>
            <ul id="navbar" class="navbar-nav">
                <li class="nav-item dropdown justify-content-end">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown_parametres" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-cog"></i> Paramétrage
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown_parametres">
                        <a class="dropdown-item" href="{{ path("user_banques") }}">Banques</a>
                        <a class="dropdown-item" href="{{ path("user_tribunaux") }}">Tribunaux</a>
                        <a class="dropdown-item" href="{{ path("user_typecomptes") }}">Types de compte</a>
                        {#<a class="dropdown-item" href="{{ path("parcelles") }}"><i class="text-success fas fa-chart-pie"></i> Parcelles</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href={{ path("familles") }}><i class="text-success fas fa-th"></i> Familles</a>
                        <a class="dropdown-item" href="{{ path("plantes") }}"><i class="text-success fas fa-seedling"></i> Plantes</a>
                        <a class="dropdown-item" href="{{ path("varietes") }}"><i class="text-success fas fa-leaf"></i> Variétés</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path("producteurs") }}"><i class="text-success fas fa-store"></i> Producteurs</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path("unites") }}"><i class="text-success fas fa-ruler"></i> Unités</a> #}
                    </div>
                </li>
            </ul>
            {% if is_granted("ROLE_ADMIN") %}
            <ul id="navbar" class="navbar-nav">
                <li class="nav-item dropdown justify-content-end">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown_parametres" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Administration
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown_parametres">
                        {# <a class="dropdown-item" href="{{ path("admin_types_date") }}"><i class="fas fa-calendar-alt text-success"></i> Types d'action</a> #}
                        {# <a class="dropdown-item" href="{{ path("admin_users") }}"><i class="fas fa-user text-success"></i> Utilisateurs</a> #}
                    </div>
                </li>
            </ul>
            {% endif %}
            <ul id="navbar" class="navbar-nav">
                <li class="nav-item dropdown justify-content-end ml-3">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown_parametres" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-user"></i> {{ app.user.email }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown_parametres">
                        <a class="dropdown-item" href="{{ path("app_logout") }}"><i class="fas fa-sign-out-alt"></i> Déconnexion</a>
                    </div>
                </li>
            </ul>
        </div>
    </nav>
</div>